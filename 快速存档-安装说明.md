# 快速存档（游戏内按 F5 立刻存档）安装说明

## 原理

游戏自带一个自动存档函数 `scr_smoothSaveAuto()`，只是没有绑定按键。
装上这个只有 19 行的模组后，**在游戏里按 F5 就会立刻存盘**，不用退回主菜单。

## 已经准备好的东西（都在这台机器上）

| 东西 | 位置 |
| --- | --- |
| ModShardLauncher（MSL）v0.13.2，自带 .NET 运行时 | `C:\Users\chpsg\Documents\ChatGPT\修改器\msl_setup\msl\ModShardLauncher.exe` |
| 快速存档模组（已编译，MSL 格式） | `...\msl_setup\msl\Mods\Quicksave.sml` |
| 游戏原版 data.win 备份 | `D:\Steam\steamapps\common\Stoneshard\data.win.原版备份_20260920` |

原版 data.win 的 SHA256：
`84034525FCDEF3C6FD9803628B8DE6C43FEA479C930E168A95906DE37A2AE19C`

## 三步安装

1. **完全退出游戏**（连 Steam 后台的游戏进程也退掉）。
2. 双击 `ModShardLauncher.exe`：
   * 第一次启动会让你选游戏目录 / `data.win`，选
     `D:\Steam\steamapps\common\Stoneshard\data.win`
   * 进左侧的 **Mods** 页面，勾上 **Quicksave**
   * 点左侧的 **铁砧图标**（Patch）等它跑完
3. 启动游戏，进到角色后按 **F5** —— 应该立刻存盘（通常屏幕右下会有存档提示）。

## 装完后的效果

* 游戏里 **F5 = 立刻存档**（不用退菜单，几乎瞬间完成）
* 加速器工具会**自动捕获**游戏写出的存档，所以 **F9 = 快速读档** 照样能用
  （快照目录：`%LOCALAPPDATA%\Stoneshard_Snapshots\`）

## 如果 MSL 打不开或者打补丁失败

MSL v0.13.2 是 2025-03 发布的，而你的 `data.win` 是 2026-09 的，它有可能不认新版。
打补丁失败**不会**损坏游戏，真要还原就把 `data.win.原版备份_20260920` 复制成
`data.win` 覆盖回去即可。

这时可以直接用加速器自带的「快速存档 (F5)」：它走的是**模拟点菜单**的路子，
一样能把当前进度存下来，只是要花 4~6 秒、期间别动鼠标。
